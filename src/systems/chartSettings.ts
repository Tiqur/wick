export default interface ChartSettings {
  upColor?: string,
  downColor?: string,
  minPrice?: number,
  maxPrice?: number,
  coordinateDelta?: number,
  bodyWidth?: number,
  wickWidth?: number,
  candleSpacing?: number,
  minHeight?: number
}

