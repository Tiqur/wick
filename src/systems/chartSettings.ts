export default interface ChartSettings {
  upColor: '#d1d4dc',
  downColor: '#3179f5',
  minPrice: number,
  maxPrice: number,
  coordinateDelta: number,
  bodyWidth: number,
  wickWidth: number,
  candleSpacing: number
}

